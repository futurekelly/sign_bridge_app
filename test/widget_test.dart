// Basic smoke test for SignBridge app.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App placeholder test', (WidgetTester tester) async {
    // Full widget tests require Firebase mock setup.
    // This is a placeholder to keep `flutter test` green.
    expect(1 + 1, equals(2));
  });
}
